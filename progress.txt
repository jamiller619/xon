## 2026-03-27 - US-035
- Implemented persistent audio player bar with queue management
- Files created: apps/web/src/components/AudioPlayer.tsx, AudioPlayer.module.css
- Files changed: apps/web/src/store/index.ts (added `useAudioStore` with full queue state and actions)
- Files changed: apps/web/src/components/Layout.tsx (renders AudioPlayer, adds padding-bottom when queue active)
- Files changed: apps/web/src/components/Layout.module.css (added `.contentWithPlayer` padding-bottom class)
- Files changed: apps/web/src/components/MediaCard.tsx (play/add-to-queue buttons on audio items in grid and list view)
- Files changed: apps/web/src/components/MediaCard.module.css (overlay and list audio button styles)
- Files changed: apps/web/src/pages/MediaDetail.tsx (play/add-to-queue actions for audio items on detail page)
- Files changed: apps/web/src/components/TopBar.tsx (fixed pre-existing biome lint errors: added `type="button"` to buttons)
- AudioPlayer: fixed-bottom bar; play/pause, prev/next, seek bar, volume, shuffle, repeat (none/all/one)
- Queue panel: toggle with count, list of tracks, reorder up/down, remove, clear all, highlights active track
- useAudioStore: Zustand store with playTrack (play immediately), addToQueue, removeFromQueue, clearQueue, moveUp/moveDown, shuffle logic
- Player hidden when queue is empty; persists across page navigation via Zustand global state
- **Learnings for future iterations:**
  - Zustand store is the right approach for persistent cross-page UI state like an audio queue
  - Use a `playingRef` to avoid re-running the track-load effect when play/pause changes
  - `audio.play()` returns a Promise — always `.catch()` to handle autoplay policy rejections
  - Content area needs `padding-bottom` matching the player bar height (64px + 24px = 88px) to avoid content hidden behind bar
  - Pre-existing biome errors in TopBar.tsx (missing `type="button"`) were blocking `yarn lint` — fix them alongside the story
---

## 2026-03-27 - US-034
- Added subtitle and audio track selection to the video player
- Files changed: packages/server-core/src/ffprobe.ts (added `extractStreamTracks` returning all audio + subtitle streams)
- Files changed: packages/server-core/src/routes/media.ts (GET /api/v1/media/:id/tracks, GET /api/v1/media/:id/subtitle endpoints)
- Files changed: apps/web/src/components/VideoPlayer.tsx (fetch tracks on mount, subtitle <track> elements, subtitle/audio dropdowns)
- Files changed: apps/web/src/components/VideoPlayer.module.css (trackControls, trackLabel, trackSelect styles)
- Files changed: packages/server-core/src/routes/media.test.ts (11 new tests; total 240 in server-core)
- `/tracks` endpoint: runs ffprobe via `extractStreamTracks`, scans dir for .srt/.vtt matching base name; returns `{ audioTracks, subtitleTracks }`
- `/subtitle` endpoint: security validates no path traversal + .srt/.vtt only; serves as `text/vtt`; prepends WEBVTT header to .srt files
- VideoPlayer: fetches tracks on mount; injects `<track>` elements for external .vtt files; subtitle dropdown syncs via `textTracks[i].mode`; audio dropdown uses `audioTracks` API (Safari/future support) with graceful fallback
- **Learnings for future iterations:**
  - `vi.mock("../ffprobe.js", ...)` is the clean way to mock ffprobe calls in route tests — avoids needing to mock `node:child_process` in media.test.ts
  - Add `readdir: vi.fn()` to the `node:fs/promises` mock factory when testing directory scanning endpoints
  - `string | undefined` in object literal is NOT assignable to `{prop?: string}` declared type — use conditional spread `...(val ? { prop: val } : {})` instead
  - HTML5 `audioTracks` API exists only in Safari; for Chrome/Firefox, audio track switching requires MSE/HLS — show dropdown but degrade gracefully
  - `<track>` elements must be children of `<video>`; set `track.mode = "showing"` via `videoElement.textTracks[i]` to activate
---

## 2026-03-27 - US-033
- Implemented HTML5 video player with streaming endpoint and keyboard shortcuts
- Files changed: packages/server-core/src/routes/media.ts (GET /api/v1/media/:id/stream with range request support), src/routes/media.test.ts (6 new streaming tests; total 229 in server-core)
- Files created: apps/web/src/components/VideoPlayer.tsx, VideoPlayer.module.css
- Files changed: apps/web/src/pages/MediaDetail.tsx (Play button triggers inline VideoPlayer for video/* mimeType)
- Streaming endpoint: uses `createReadStream` + `Readable.toWeb()` for web-compatible streaming; supports `Range` header for HTTP 206 partial content; returns 416 for out-of-bounds ranges
- VideoPlayer: HTML5 `<video controls autoPlay>` with src pointing to /stream; keyboard shortcuts via document keydown listener (space=play/pause, arrows=seek±5s, F=fullscreen, Escape=close)
- Play button disabled for non-video mimeTypes and DRM-protected items; VideoPlayer replaces poster in hero section
- 6 new streaming tests covering: 404 for missing item, 404 for inaccessible file, 200 full response, 206 range request, 206 open-ended range, 416 out-of-bounds
- **Learnings for future iterations:**
  - `createReadStream` from `node:fs` + `Readable.toWeb()` from `node:stream` converts a Node.js stream to a web ReadableStream for Hono `c.body()`
  - Range request pattern: parse `Range: bytes=start-end` header; respond 206 with `Content-Range: bytes start-end/total` and `Content-Length: chunkSize`
  - In tests, mock `node:fs` with `createReadStream: vi.fn()` returning `Readable.from(["data"])` — `Readable.toWeb()` works on `Readable.from()` streams
  - Add `stat: vi.fn()` to the `node:fs/promises` mock factory for streaming route tests; mock `node:fs` separately
  - Biome `useNumberNamespace`: use `Number.parseInt` not global `parseInt`
  - Biome a11y `useMediaCaption`: suppress with `biome-ignore lint/a11y/useMediaCaption: ...` comment on `<video>` elements without static captions
---

## 2026-03-27 - US-032
- Added inline metadata editing to media detail page
- Files changed: apps/web/src/pages/MediaDetail.tsx (edit form with title/description/tags), MediaDetail.module.css (edit form styles, tags display, titleRow layout)
- Files changed: packages/server-core/src/routes/media.ts (PUT /api/v1/media/:id endpoint), src/routes/media.test.ts (5 new tests)
- PUT endpoint accepts `{ title?, description?, tags? }`; tags merged into existing metadata JSON preserving other fields; returns updated item with thumbnailUrls
- Frontend: Edit button toggles form; Cancel reverts to view mode; Save sends PUT and updates local state; tags displayed as chips in view mode
- Tags excluded from generic metadata display (rendered as their own chips UI)
- 5 new tests; total 223 tests in server-core
- **Learnings for future iterations:**
  - PUT endpoint for media uses same pattern as libraries PUT: fetch existing, build `updates` object, call `db.update().set(updates)`
  - Tags live inside the `metadata` JSON column under key `tags`; PUT must parse existing metadata, update `tags` key, and re-serialize to preserve other fields
  - Biome line-length limit causes long JSX attributes to be expanded to multi-line; run `biome format --write` after writing TSX
---

## 2026-03-27 - US-031
- Implemented media detail page at /media/:id
- Files created: apps/web/src/pages/MediaDetail.tsx, MediaDetail.module.css
- Files changed: apps/web/src/App.tsx (import MediaDetail for /media/:id route)
- Detail page fetches GET /api/v1/media/:id (already existed); shows large poster/thumbnail, title, description, category badge
- Action buttons: Play (disabled if DRM-protected), Favorite, Add to Collection (all functional-looking but placeholders)
- DRM-protected items: lock icon overlay on poster, red DRM notice with explanatory text, disabled Play button with tooltip
- Metadata table: shows category, format (mimeType), file size, file name, date added, last scanned, plus all fields from the `metadata` JSON column
- Arrays in metadata (e.g. genre, tags) joined with ", " for display
- Related items section rendered as placeholder text
- Loading skeleton with shimmer animation; error state with back link
- **Learnings for future iterations:**
  - `GET /api/v1/media/:id` already returns `thumbnailUrls` (added by `withThumbnailUrls`) — use `large` URL for poster on detail page
  - `metadata` is a JSON string column; parse with try/catch and display key/value pairs; filter out arrays separately for join display
  - Biome prefers single-line JSX for short conditional expressions: `{cond && <El />}` not `{cond && (\n  <El />\n)}`
---

## 2026-03-27 - US-030
- Added filter bar and active filter chips to LibraryBrowser page
- Files changed: apps/web/src/pages/LibraryBrowser.tsx, LibraryBrowser.module.css, packages/server-core/src/routes/libraries.ts
- Filter bar has 6 controls: Category (functional), Sort (functional), Genre/Year/Rating/Tags (disabled placeholders for future use)
- Category filter maps to `mediaCategory` query param already supported by the library media API
- Sort dropdown replaces/augments column-header sorting; options: Date Added, Title A-Z/Z-A, File Size, Release Date, Rating
- Active filters shown as removable chips; "Clear all" button appears when multiple filters active
- Server: added `rating` sort via `json_extract(metadata, '$.rating')` alongside existing `releaseDate` sort
- **Learnings for future iterations:**
  - `URLSearchParams` is clean for building query strings with optional params (`params.set(key, val)` only when val is set)
  - For disabled select elements, combine `.filterSelect` + `.filterDisabled` classes; TypeScript is fine with disabled selects
  - SORT_OPTIONS array of `{ label, col, dir }` objects + a `makeSortKey(col, dir)` string serializer is an ergonomic pattern for controlled sort dropdowns
---

## 2026-03-27 - US-028
- Implemented library browser page at /libraries/:id with responsive grid view
- Files created: apps/web/src/pages/LibraryBrowser.tsx, LibraryBrowser.module.css
- Files changed: apps/web/src/App.tsx (import LibraryBrowser for /libraries/:id route), apps/web/src/components/MediaCard.tsx (added fileType display), MediaCard.module.css (added .meta and .fileType styles)
- LibraryBrowser fetches library info (name) and media items separately; page-based pagination with prev/next buttons
- Pagination: 40 items per page; shows next button when a full page is returned (heuristic); hides pagination when only 1 page
- Loading skeleton: 40 shimmer skeleton cards with CSS animation while data loads
- MediaCard now shows mimeType subtype (e.g. "MP4" from "video/mp4") as a small secondary badge
- **Learnings for future iterations:**
  - For page-based pagination without a total-count endpoint, use heuristic: if returned count === PAGE_SIZE, assume there may be more pages
  - CSS shimmer skeleton: `background: linear-gradient(90deg, ...)` with `background-size: 200%` + `animation` gives smooth loading pulse
  - `biome-ignore lint/suspicious/noArrayIndexKey` comment needed for skeleton arrays using index keys (no stable IDs available)
---

## 2026-03-27 - US-027
- Implemented Dashboard landing page at / route
- Files created: apps/web/src/pages/Dashboard.tsx, Dashboard.module.css, components/MediaCard.tsx, MediaCard.module.css
- Files changed: apps/web/src/App.tsx (import Dashboard), packages/server-core/src/routes/media.ts (added GET /media global list endpoint)
- Dashboard fetches recently added (GET /api/v1/media?order=desc&limit=20) and per-library media (GET /api/v1/libraries/:id/media?order=desc&limit=8) in parallel
- "Continue watching" section shows empty placeholder (no progress API yet)
- MediaCard component: thumbnail image (falls back to placeholder), title, category badge; links to /media/:id
- Grid layout: CSS grid with auto-fill minmax(160px, 1fr) for responsive cards
- GET /api/v1/media global endpoint added to media router; supports sortBy, order (default desc), page, limit params
- **Learnings for future iterations:**
  - Pages go in `apps/web/src/pages/`, shared UI components in `apps/web/src/components/`
  - `Object.fromEntries(entries)` with `as const` tuples from `Promise.all(libs.map(...))` is the idiomatic way to build a Record from parallel fetches
  - MediaCard is a reusable card component shared by Dashboard and future library views
---

## 2026-03-27 - US-026
- Implemented app shell: Layout, Sidebar, TopBar components with CSS Modules
- Files created: apps/web/src/components/Layout.tsx, Layout.module.css, Sidebar.tsx, Sidebar.module.css, TopBar.tsx, TopBar.module.css, src/vite-env.d.ts
- Files changed: apps/web/src/App.tsx (added Layout as layout route wrapping all routes)
- Sidebar fetches libraries from `/api/v1/libraries` via useEffect; shows loading/empty states
- TopBar has search input placeholder and user menu placeholder (button with avatar)
- Responsive: sidebar is fixed-position off-screen on mobile (<768px); hamburger button in TopBar toggles it via Zustand `sidebarOpen` state; overlay closes it on click
- CSS Modules: dark theme (#0f0f1a background, #6c63ff accent), active NavLink highlighted with left border + accent color
- **Learnings for future iterations:**
  - `noUncheckedIndexedAccess: true` causes CSS Module property accesses (e.g. `styles.foo`) to return `string | undefined`; use `styles.foo ?? ""` for direct className props; template literals (`\`${styles.foo}\``) produce `string` and work without fallback
  - Add `src/vite-env.d.ts` with `/// <reference types="vite/client" />` to get CSS Module type declarations (without it, TypeScript can't resolve `*.module.css` imports)
  - React Router v7 `<Outlet />` in a layout route: use `<Route element={<Layout />}>` wrapping child routes in App.tsx
---

## 2026-03-27 - US-025
- Scaffolded `apps/web` React web app with Vite 6, React 19, React Router 7, Zustand 5, CSS Modules
- Files created: apps/web/package.json, apps/web/tsconfig.json, apps/web/vite.config.ts, apps/web/index.html, apps/web/src/main.tsx, apps/web/src/App.tsx, apps/web/src/store/index.ts
- Also updated root tsconfig.json to exclude apps/web (avoids NodeNext/JSX conflicts)
- Dev server proxies `/api` to `http://localhost:32400` via vite.config.ts `server.proxy`
- Vite build outputs to `apps/web/dist` — set `WEB_CLIENT_DIR=apps/web/dist` for server-core static serving
- Zustand store (`src/store/index.ts`) has `sidebarOpen`/`viewMode` state as skeleton for UI stories
- CSS Modules: Vite supports `*.module.css` natively, no extra config needed
- App routes: /, /libraries/:id, /media/:id, /search, /admin — all with placeholder elements
- Web app typecheck uses `tsc` with `noEmit: true` in tsconfig (not `tsc --build --noEmit`); `moduleResolution: "bundler"` for Vite
- **Learnings for future iterations:**
  - Root `tsc --build` scans all TS files not in `exclude` — must add `apps/web` to root tsconfig exclude to prevent NodeNext settings from being applied to JSX/DOM files
  - Web app tsconfig uses `moduleResolution: "bundler"` + `lib: ["ES2022", "DOM", "DOM.Iterable"]` + `jsx: "react-jsx"` + `noEmit: true`; standalone (not composite, not in root references)
  - With bundler resolution, imports don't need `.js` extensions (unlike NodeNext)
  - CSS Modules work out-of-the-box in Vite for files named `*.module.css`
---

## 2026-03-27 - US-023
- Implemented DRM detection in `drm.ts` for FairPlay (M4V/M4A), Widevine (MP4/M4V), and Adobe ADEPT (EPUB)
- Files changed: packages/server-core/src/drm.ts (new), src/drm.test.ts (new), src/orchestrator.ts (calls detectDrm for all new/changed files), src/index.ts (exports detectDrm)
- `detectDrm(filePath)` calls FairPlay, Widevine, and Adobe detectors in parallel via `Promise.all`; returns false on any error (non-blocking)
- FairPlay: opens M4V/M4A, walks ISO BMFF boxes at top level, returns true if `drms` box found
- Widevine: opens MP4/M4V/M4A/MOV, linearly scans first 64KB for `pssh` box, checks 16-byte System ID against Widevine UUID
- Adobe DRM: opens EPUB (ZIP), finds EOCD, iterates central directory for `META-INF/encryption.xml`, checks content for Adobe ADEPT namespace
- Orchestrator: `detectDrm` called for every new file before insert and every changed file before update; result stored in `drmProtected` column
- `drmProtected` column was already present in schema from US-007 — no migration needed
- 14 new tests in drm.test.ts (including ZIP builder helper); total 218 tests in server-core
- **Learnings for future iterations:**
  - `node:fs/promises` `open()` returns a `FileHandle` — mock it by providing `{ read, stat, close }` methods; `read` signature is `(buf, offset, length, position) => { bytesRead }`
  - ISO BMFF box walking: each box = size(4BE) + type(4ASCII); size includes the 8-byte header; iterate at `offset += size`
  - Widevine PSSH: linear scan is simpler than recursive box parsing — search for `pssh` bytes at positions [i+4..i+7], then read SystemID at [i+12..i+27]
  - ZIP central directory: EOCD at end of file gives `cdOffset` and `cdSize`; CD entries have 46-byte fixed header + variable filename/extra/comment; local file offset at CD entry byte 42
  - `EPUB` Adobe DRM: only need to check `encryption.xml` string content for the Adobe ADEPT namespace — no XML parsing required
---

## 2026-03-27 - US-022
- Implemented scheduled and filesystem-watch scan triggers
- Files changed: src/schema.ts (added scanSchedule column), src/scheduler.ts (new), src/scheduler.test.ts (new), src/routes/scan.ts (added PUT /schedule endpoint), src/routes/scan.test.ts (4 new tests), src/server.ts (starts scheduler on boot), src/index.ts (exports scheduler), drizzle/0003_neat_speed.sql (migration)
- `scheduler.ts`: `parseCronInterval(expr)` parses two patterns: `*/N * * * *` (every N minutes) and `0 */N * * *` (every N hours); returns interval in ms or null for unsupported expressions
- `startScheduler(db, trigger?)`: loads all libraries from DB, sets up `setInterval` for scheduled libs and `fs.watch` for local enabled data sources; returns `SchedulerHandle` with `stop()`
- Debounce: file change events use 2-second `setTimeout` per library; rapid changes collapse to a single scan trigger
- `PUT /api/v1/libraries/:id/scan/schedule`: validates cron expression via Zod refine + `parseCronInterval`; persists to `scan_schedule` column; returns updated library
- `stop()` clears all interval timers, debounce timers, and fs.watch handles
- 14 new tests (10 in scheduler.test.ts, 4 in scan.test.ts); total 204 tests in server-core
- **Learnings for future iterations:**
  - `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()` for testing setInterval-based scheduling without real time
  - Mock `node:fs` with `vi.mock("node:fs", () => ({ watch: vi.fn() }))` to capture the watch callback for debounce tests
  - Biome `noNonNullAssertion` applies in test files too — use `?.()` instead of `!()` for optional function calls after `expect().toBeDefined()`
  - `watch()` from `node:fs` (not `node:fs/promises`) for filesystem watching; `{ recursive: true }` passes the dataSource's `recursive` flag
  - `startScheduler` must be awaited (async) since it fetches libraries and data sources from the DB before setting up timers/watchers
---

## 2026-03-27 - US-021
- Implemented WebSocket channel for real-time events at /api/v1/ws
- Files changed: packages/server-core/src/events.ts (new), src/events.test.ts (new), src/routes/ws.ts (new), src/routes/ws.test.ts (new), src/routes/scan.ts (emits scan events), src/orchestrator.ts (emits media:added/removed), src/server.ts (sets up WebSocket upgrade handler), src/index.ts (exports events), package.json (added ws + @types/ws)
- `events.ts`: module-level EventEmitter `eventBus` + `emitEvent(event: XonEvent)` helper; `XonEvent` union type covers scan:progress, scan:complete, scan:error, media:added, media:removed
- `routes/ws.ts`: `createWsServer()` factory using `ws` package with `noServer: true`; maintains per-connection event listener; cleans up on close
- `server.ts`: after `serve()`, sets up `upgrade` event handler to route `/api/v1/ws` to the WebSocket server
- scan.ts: emits scan:progress (with percentComplete calc), scan:complete, scan:error events via eventBus
- orchestrator.ts: emits media:added after each insert; queries IDs before delete to emit media:removed per item
- `@hono/node-server` v1.19.11 has no `/ws` export — used `ws` npm package directly with noServer mode and HTTP upgrade event
- 11 new tests (7 in events.test.ts, 4 in ws.test.ts); total 190 tests in server-core
- **Learnings for future iterations:**
  - `@hono/node-server` v1.19.11 (latest stable) has no WebSocket support — use `ws` package with `noServer: true` + `server.on("upgrade", ...)` pattern
  - `ws.WebSocketServer({ noServer: true })` + `handleUpgrade(req, socket, head, cb)` gives full control over which HTTP upgrade requests become WebSocket connections
  - Module-level `EventEmitter` (eventBus) is the cleanest way to decouple event producers (scan, orchestrator) from consumers (WebSocket clients)
  - To test WebSocket logic: create a raw `http.createServer()`, attach the WebSocketServer upgrade handler, listen on port 0, connect with `ws.WebSocket` client
  - `collectMessages(ws, count)` pattern with a timeout fallback avoids hanging tests when fewer messages arrive than expected
---

## 2026-03-27 - US-020
- Implemented media items list and detail API endpoints
- Files changed: packages/server-core/src/routes/media.ts (added GET /:id, added withThumbnailUrls helper), src/routes/libraries.ts (added GET /:libraryId/media), src/routes/media.test.ts (3 new tests), src/routes/libraries.test.ts (9 new tests for media list)
- `GET /api/v1/media/:id` returns full media item with computed `thumbnailUrls` field (object with small/medium/large URL strings, or null if no thumbnails)
- `GET /api/v1/libraries/:libraryId/media` supports filtering (mediaCategory, mimeType, drmProtected), sorting (title, fileSize, releaseDate via json_extract, default dateAdded), pagination (page/limit, max 100 per page)
- `withThumbnailUrls(item)` exported from media.ts and imported into libraries.ts to avoid duplication
- Sorting uses ternary chain to avoid `noImplicitAnyLet` biome error (no `let orderExpr` without type annotation)
- releaseDate sorting uses `sql\`json_extract(${mediaItems.metadata}, '$.releaseDate')\`` — passes drizzle's `asc()`/`desc()` directly
- 12 new tests; total 179 tests in server-core
- **Learnings for future iterations:**
  - Biome `noImplicitAnyLet` applies to switch-assigned `let` variables too — use ternary chain or initialize with default value to avoid needing explicit type import
  - `asc()`/`desc()` from drizzle-orm accept SQL template literals directly for computed sort expressions
  - `withThumbnailUrls` helper pattern: spread item then add computed fields — export from one route file, import into others (no circular deps since media.ts doesn't import libraries.ts)
  - For library-scoped list endpoints, mount directly on the libraries router with `router.get("/:libraryId/resource", ...)` — no sub-router needed for a single GET endpoint
---

## 2026-03-27 - US-019
- Implemented thumbnail serving API endpoint GET /api/v1/media/:id/thumbnail?size=small|medium|large
- Files changed: packages/server-core/src/routes/media.ts (new), src/routes/media.test.ts (new), src/app.ts (mounted media router)
- `makeMediaRouter(db)` factory; validates size param (small/medium/large, defaults to medium); looks up mediaItem by id; parses thumbnailPaths JSON; reads file from disk; returns with Content-Type: image/jpeg and Cache-Control: public, max-age=86400, immutable
- Returns 404 for unknown id, null thumbnailPaths, invalid JSON, or missing file on disk; 400 for invalid size param
- `new Uint8Array(buffer)` required for `c.body()` since Hono expects `Uint8Array<ArrayBuffer>` not `Buffer<ArrayBufferLike>`
- `const item = rows[0]; if (!item)` pattern avoids non-null assertion while satisfying `noUncheckedIndexedAccess`
- 9 new tests in media.test.ts; total 167 tests in server-core
- **Learnings for future iterations:**
  - Hono `c.body()` accepts `Uint8Array<ArrayBuffer>` not `Buffer<ArrayBufferLike>` — use `new Uint8Array(buffer)` to convert Node.js Buffer
  - Biome `noNonNullAssertion` forbids `arr[0]!` — use `const x = arr[0]; if (!x) return;` pattern for early return after index access
---

## 2026-03-27 - US-018
- Implemented video thumbnail generation using FFmpeg + sharp in `videoThumbnails.ts`
- Files changed: packages/server-core/src/videoThumbnails.ts (new), src/videoThumbnails.test.ts (new), src/orchestrator.ts (calls generateVideoThumbnails for video items), src/index.ts (exports)
- `generateVideoThumbnails(filePath, mediaItemId, dataDir)`: 1) uses ffprobe to get duration, 2) spawns ffmpeg to extract frame at 10% of duration, 3) uses sharp to resize to 3 sizes (same as image thumbnails)
- `isVideoCategory(category)` returns true for Movies/TVShows/Clips/HomeVideos
- FFmpeg invoked as: `ffmpeg -ss <timestamp> -i <file> -frames:v 1 -q:v 2 -y <tmpPath>`
- Temp frame file cleaned up after sharp resizing (both success and failure paths)
- Falls back to timestamp=0 when ffprobe duration is unavailable; graceful null return on any failure
- Orchestrator: calls generateVideoThumbnails for new/changed video items (else-if after image branch)
- 13 new tests in videoThumbnails.test.ts; total 158 tests in server-core
- **Learnings for future iterations:**
  - When spawn is mocked with `mockReturnValueOnce`, the proc is created BEFORE the actual spawn call - any `setTimeout` scheduled at creation fires before listeners are added. Use `mockImplementation` with `Promise.resolve().then(...)` to emit events after listeners attach.
  - `Promise.resolve().then(...)` is a microtask; it fires AFTER synchronous listener registration but BEFORE `await` yields control back — perfect for mock event emission timing.
  - MediaCategory enum values have spaces in the string value (e.g., `TVShows = "TV Shows"`) — always use `MediaCategory.X` not the enum key string in tests.
---

## 2026-03-27 - US-017
- Implemented thumbnail generation for image media items using `sharp` library
- Files changed: packages/server-core/src/thumbnails.ts (new), src/thumbnails.test.ts (new), src/orchestrator.ts (calls generateThumbnails for image items), src/schema.ts (added thumbnailPaths column), src/index.ts (exports), drizzle/0002_aromatic_hobgoblin.sql (migration), package.json (added sharp dep)
- `generateThumbnails(filePath, mediaItemId, dataDir)` generates 3 sizes (small=150px, medium=300px, large=600px) using sharp's `fit: "inside", withoutEnlargement: true` mode; saves as JPEG with quality 80
- Thumbnails stored at `<dataDir>/thumbnails/<mediaItemId>_<size>.jpg`; creates thumbnails dir with `mkdir -p`
- Returns null on mkdir failure or sharp processing error (graceful fallback)
- Schema updated: added `thumbnailPaths text` nullable column to `media_items`; migration 0002 adds the column via `ALTER TABLE`
- Orchestrator: for new image items, generates thumbnails before insert and stores JSON-serialized paths; for changed items, fetches existing DB id then regenerates thumbnails
- `scanLibrary` now accepts optional `dataDir` parameter (defaults to `process.env.DATA_DIR ?? "./data"`)
- 6 new tests in thumbnails.test.ts; total 145 tests in server-core
- **Learnings for future iterations:**
  - `sharp` default import (ESM): `import sharp from "sharp"` — mock with `vi.mock("sharp", () => ({ default: mockFn }))`
  - `sharp(file).clone().resize(w, h, { fit: "inside", withoutEnlargement: true }).jpeg().toFile(path)` pattern for batch-safe thumbnail generation; use `clone()` per size to reuse the same sharp instance
  - Drizzle `ALTER TABLE ADD COLUMN` migration generated cleanly by drizzle-kit after schema update; `yarn workspace @xon/server-core db:generate` handles it
  - `@types/sharp` is bundled with sharp — no separate `@types/sharp` package needed
---

## 2026-03-27 - US-016
- Implemented metadata extraction for Documents, Fonts, 3D Models, and Archives in `miscmeta.ts`
- Files changed: packages/server-core/src/miscmeta.ts (new), src/miscmeta.test.ts (new), src/orchestrator.ts (calls misc extractors), src/index.ts (exports)
- Documents: ExifTool extracts PageCount, Author, Title via shared `runExiftool()` helper
- Fonts: ExifTool extracts FamilyName/FontSubfamily/NumGlyphs; subfamily string parsed into fontWeight/fontStyle
- 3D Models: OBJ files parsed with `readFile` counting "v " (vertex) and "f " (face) lines; glTF JSON parsed via POSITION accessor count and indices/3 for face count; unsupported formats return `{}`
- Archives: ZIP parsed by reading EOCD record from end of file then iterating central directory entries; TAR parsed with 512-byte header blocks; .tgz/.tar.gz decompressed with `gunzipSync` before TAR parsing; unsupported formats return `{}`
- 27 new tests in miscmeta.test.ts; total 139 tests in server-core
- **Learnings for future iterations:**
  - Mocking `node:fs/promises` with `vi.mock("node:fs/promises", () => ({ readFile: vi.fn() }))` works cleanly alongside `node:child_process` mocks
  - ZIP EOCD signature `0x06054b50` is at the end of file (before comment); central directory starts at cdOffset with `0x02014b50` entries at fixed 46-byte stride + variable filename/extra/comment
  - TAR headers are 512-byte blocks; filename at bytes 0-99, size in octal at 124-135, typeflag at 156 ('0'/NUL = regular file); USTAR prefix at 345 when magic "ustar" is at 257
  - `gunzipSync` from `node:zlib` can decompress a gzipped TAR buffer before parsing; detect gzip by magic bytes `0x1f 0x8b`
  - ExifTool shared helper `runExiftool(filePath)` returns `Record<string, unknown> | null` — avoids repeating spawn boilerplate for doc/font extractors
  - `block[156]` in Buffer is a number, not a character — compare with `0x30` (ASCII '0') not `'0'`
---

## 2026-03-27 - US-015
- Implemented `music-metadata` npm library for ID3/Vorbis tag parsing for Music and Audiobook categories
- Files changed: packages/server-core/src/musictags.ts (new), src/musictags.test.ts (new), src/orchestrator.ts (calls extractMusicTags before ffprobe check), src/index.ts (exports), package.json (added music-metadata dep)
- `extractMusicTags(filePath)` calls `parseFile()` from `music-metadata`, extracts: title, artist, album, year, genre (first element), trackNumber, discNumber, hasAlbumArt, duration, bitrate, sampleRate, channels, codec
- `isMusicCategory(category)` returns true for Music and Audiobooks only (not AudioClips or Podcasts)
- Orchestrator priority: `isMusicCategory` checked first → music-metadata; then `isAudioVideoCategory` → ffprobe; then `isImageCategory` → exiftool
- Music and Audiobooks now use music-metadata (which provides both technical metadata AND tags) instead of ffprobe
- 13 new tests in musictags.test.ts; total 112 tests in server-core
- **Learnings for future iterations:**
  - `music-metadata` `parseFile()` returns `{ common, format, native, quality }` — use `common` for tags, `format` for technical info
  - `common.track` and `common.disk` always exist with `{ no: number | null; of: number | null }` — no `?.` needed
  - `common.genre` is `string[]` not `string` — take `[0]` for first genre
  - Mocking `music-metadata` with `vi.mock('music-metadata', () => ({ parseFile: vi.fn() }))` works cleanly; cast mock return with `as never`
  - Biome import sort: `@xon/shared` (scoped package) sorts before `music-metadata` (plain package) — let `biome check --write` fix it
---

## 2026-03-27 - US-014
- Implemented ExifTool metadata extraction for image media items (Pictures, Images, DesignFiles)
- Files changed: packages/server-core/src/exiftool.ts (new), src/exiftool.test.ts (new), src/orchestrator.ts (calls exiftool on insert/update), src/index.ts (exports)
- `extractExiftoolMetadata(filePath)` spawns `exiftool -json -n` and parses the JSON array output
- Extracts: width, height (ImageWidth/ImageHeight), colorSpace (ColorSpace/ColorSpaceData), cameraModel (Model/CameraModelName), gpsLatitude, gpsLongitude (GPSLatitude/GPSLongitude with -n flag returns numeric values), dateTaken (DateTimeOriginal → CreateDate fallback), orientation (string or number → stored as string)
- Returns `null` on ENOENT (exiftool not installed), non-zero exit, or JSON parse failure; returns `{}` for empty array output
- Orchestrator updated: `else if (isImageCategory(...))` branch calls exiftool for new and changed files
- 12 new tests in exiftool.test.ts; total 99 tests in server-core
- **Learnings for future iterations:**
  - ExifTool outputs a JSON *array* (one entry per file) — access `parsed[0]` for single-file invocations
  - `exiftool -n` flag returns numeric values for GPS coords and orientation (not formatted strings); orientation becomes a number like `6` instead of `"Rotate 90 CW"`
  - Biome may reformat multi-line named imports to single line if under 100 chars — let `biome check --write` decide
---

## 2026-03-27 - US-013
- Implemented FFprobe metadata extraction for audio and video media items
- Files changed: packages/server-core/src/ffprobe.ts (new), src/ffprobe.test.ts (new), src/orchestrator.ts (calls ffprobe on insert/update), src/index.ts (exports)
- `extractFfprobeMetadata(filePath)` spawns `ffprobe -v quiet -print_format json -show_streams -show_format` and parses output
- Extracts: duration, bitrate (from format), codec+resolution (from video stream), audioCodec+sampleRate+channels (from audio stream)
- Returns `null` on ENOENT (ffprobe not installed), non-zero exit, or JSON parse failure — logged with `console.error`
- `isAudioVideoCategory(category)` checks against Movies/TVShows/Clips/HomeVideos + Music/Audiobooks/AudioClips/Podcasts
- Orchestrator calls `extractFfprobeMetadata` for new and changed files; stores result as JSON in `metadata` column; skips update if null
- Existing orchestrator tests still pass — ffprobe fails on fake binary files but returns null gracefully
- 11 new tests in ffprobe.test.ts; total 87 tests in server-core
- **Learnings for future iterations:**
  - `exactOptionalPropertyTypes: true` prevents assigning `string | undefined` to `string` optional property — use `if (typeof x === "string") obj.prop = x`
  - Biome `useLiteralKeys`: use `obj.sample_rate` not `obj["sample_rate"]` even for snake_case keys on `Record<string, unknown>`
  - To mock `node:child_process` spawn in vitest: create EventEmitter with `.stdout` EventEmitter; cast to `ReturnType<typeof spawn>`
---

## 2026-03-27 - US-012
- Implemented scan trigger API endpoints
- Files changed: packages/server-core/src/routes/scan.ts (new), src/routes/scan.test.ts (new), src/routes/libraries.ts (mount scan router)
- `POST /api/v1/libraries/:id/scan` — starts async scan, returns 202 `{status:"started"}` or 409 `{status:"already_running"}`
- `GET /api/v1/libraries/:id/scan/status` — returns `{status, startedAt, progress, summary, error}` or `{status:"idle"}` if no scan
- In-memory `Map<libraryId, ScanState>` registry in `makeScanRouter` closure; scan runs via unresolved `.then()/.catch()` chain
- `vi.mock("../orchestrator.js")` used to control scanLibrary timing in tests; dynamic import with `await import(...)` after mock
- 7 new tests, all passing (total 76 tests in server-core)
- **Learnings for future iterations:**
  - In vitest, mocked module values settle after 2x `await Promise.resolve()` — use this to flush microtask queue in tests
  - `vi.mock(...)` is hoisted; use `await import(...)` after mock call to get typed reference to the mocked function
  - Hono sub-router `GET /status` correctly matches `/:libraryId/scan/status` when mounted at `/:libraryId/scan`
---

## 2026-03-27 - US-011
- Implemented scan orchestrator that saves discovered files as media items in the database
- Files changed: packages/server-core/src/orchestrator.ts (new), src/orchestrator.test.ts (new), src/index.ts (exports)
- `scanLibrary(db, libraryId, onProgress?)` fetches enabled data sources, calls `scanDataSource()` per source, inserts/updates/deletes media items
- `ScanProgress` type: `{ dataSourceId, totalFiles, processedFiles, currentFile }` — callback invoked before each file insert/update
- `ScanSummary` return: `{ libraryId, newItems, updatedItems, removedItems, totalDiscovered }`
- Disabled data sources are skipped; throws if library not found
- 9 new tests all passing (total 69 tests in server-core)
- **Learnings for future iterations:**
  - `inArray(col, values)` from drizzle-orm requires a non-empty array — always guard with `if (arr.length > 0)` before calling
  - Unused `vi` import from vitest triggers Biome noUnusedImports — remove it if not mocking
  - Import ordering in orchestrator: local `./scanner.js` sorts before `./schema.js` alphabetically in Biome
---

## 2026-03-27 - US-010
- Implemented filesystem scanner for media file discovery
- Files changed: packages/server-core/src/scanner.ts (new), src/scanner.test.ts (new), src/index.ts (exports), package.json (added @xon/media-types dep)
- `scanDataSource({ path, recursive }, existingItems?)` walks directory using `node:fs/promises` `readdir`/`stat`
- Identifies media files by extension via `getMediaCategory` from `@xon/media-types`; skips non-media extensions
- Returns `ScanResult` with `discovered`, `newFiles`, `changedFiles`, `removedFilePaths` by comparing against existing items by filePath+fileSize
- Handles permission errors and inaccessible paths gracefully with `console.error` logging (returns empty array)
- 9 new tests all passing (total 60 tests in server-core)
- **Learnings for future iterations:**
  - `.txt` IS a media type in @xon/media-types (Documents category) — don't assume it's non-media in tests; use `.log` or other non-standard extension
  - `tsc --build --noEmit` fails in TypeScript 5.9 for composite project reference chains — use root-level `yarn typecheck` (`tsc --build` without `--noEmit`) for type checking
  - `@xon/media-types` does NOT need to be in server-core's tsconfig.json references since it's already built; adding it breaks `--noEmit` mode
  - Biome import order: `import type` from node built-ins sorts BEFORE `import` from same module group
  - Biome `noImplicitAnyLet`: uninitialized `let` variables need explicit type annotations (e.g., `let x: Dirent[]`)
---

## 2026-03-27 - US-009
- Implemented Data Sources CRUD API endpoints nested under libraries
- Files changed: packages/server-core/src/routes/sources.ts (new), src/routes/sources.test.ts (new), src/routes/libraries.ts (added sources sub-router mount)
- POST/GET/PUT/DELETE /api/v1/libraries/:libraryId/sources with Zod validation
- Local type path validation uses `node:fs/promises` `access()` — returns 400 if path inaccessible
- Sources router mounted via `router.route("/:libraryId/sources", makeSourcesRouter(db))` in libraries router
- 17 new tests all passing (total 51 tests in server-core)
- **Learnings for future iterations:**
  - When sub-router uses params from parent route (e.g., `:libraryId`), TypeScript types `c.req.param("libraryId")` as `string | undefined` — use `as string` cast since route guarantees it
  - Drizzle boolean `{ mode: "boolean" }` returns JS `true`/`false` (not `1`/`0`) in select results — test assertions must use `toBe(false)` not `toBe(0)`
  - `node:fs/promises` `access(path)` throws if path doesn't exist — wrap in try/catch for validation
---

## 2026-03-27 - US-008
- Implemented Libraries CRUD API endpoints with Zod validation
- Files changed: packages/server-core/src/app.ts (createApp factory), src/server.ts (async boot with db), src/routes/libraries.ts (new), src/routes/libraries.test.ts (new), package.json (added zod, @hono/zod-validator)
- POST/GET/GET:id/PUT/DELETE /api/v1/libraries with Zod validation on POST and PUT
- app.ts refactored: exports `createApp(db?: LibSQLDatabase)` factory and `app = createApp()` singleton for health-check tests
- server.ts now async: opens db, runs migrations, creates full app with routes, then starts server
- 14 new tests all passing (total 34 tests in server-core)
- **Learnings for future iterations:**
  - Hono `app.route("/path", router)` mounts sub-routers cleanly; use `makeXxxRouter(db)` factory pattern for route modules
  - `createApp(db?)` optional db pattern: health endpoint works without db; feature routes added conditionally
  - @hono/zod-validator: `zValidator("json", schema)` middleware, access validated body via `c.req.valid("json")`
  - Drizzle single-row lookup: use `db.select().from(table).where(eq(...))` returns array; check `rows.length === 0` for not-found
  - `crypto.randomUUID()` is available globally in Node 22+ without import
  - boot() must be async (or use `.then()`) when opening database before starting server
---

## Codebase Patterns
- Drizzle migrations: `drizzle-orm/libsql/migrator` `migrate(db, { migrationsFolder })` applies SQL files; path uses `dirname(fileURLToPath(import.meta.url))` to work in both src/ and dist/
- Drizzle schema for SQLite: use `drizzle-orm/sqlite-core` (sqliteTable, text, integer); integer with `{ mode: "timestamp" }` for dates; integer with `{ mode: "boolean" }` for booleans; FK via `.references(() => table.col, { onDelete: "cascade" })`
- JSON columns in SQLite: store as text with `.default("[]")` and handle serialization in app code
- Monorepo with Yarn 1.22 workspaces: apps/*, packages/*, plugins/*
- drizzle-orm 0.40.x + @libsql/client 0.14.x: `drizzle(client)` from `drizzle-orm/libsql`; WAL mode via `client.execute("PRAGMA journal_mode=WAL")`
- drizzle.config.ts lives at package root (not src/); excluded from tsc since `include: ["src/**/*"]`; processed directly by drizzle-kit
- Biome import sorting: node: built-ins first, then third-party (alphabetical within groups)
- TypeScript with strict mode, NodeNext module resolution (ESM)
- Biome for linting/formatting (not ESLint/Prettier)
- Node version: v24.14.0 (despite PRD saying Node 22.x LTS, .nvmrc had v24.14.0)
- tsconfig.json uses composite: true for project references support
- Each new package needs: package.json (type:"module", exports with .js extensions), tsconfig.json (extends root), src/index.ts
- When adding a package, add its path to root tsconfig.json "references" array and run `yarn install`
- Use `yarn biome check <dir>` (not `yarn check`) for linting; `yarn check` validates workspace deps
- Node built-ins (e.g., node:path) require `@types/node` as a devDependency in each package that uses them
- Test framework: vitest (^2.1.0) — handles TypeScript/ESM natively without separate tsconfig; add per-package
- noUncheckedIndexedAccess: Record<string, T> indexing returns T | undefined — use `?? fallback` pattern
- Biome line width is 100; arrays/params exceeding this are auto-reformatted to multi-line on `biome check --write`
- Biome prefers `process.env.PORT` (literal key) over `process.env["PORT"]` (computed key) — use dot notation
- Hono: use `new Hono().basePath("/api/v1")` for prefixed routes; test with `app.request("/api/v1/...")` (no server needed)
- @hono/node-server: `serve({ fetch: app.fetch, port }, callback)` returns a Node.js HTTP server with `.close()`
- Packages with cross-package deps need `references` in their own tsconfig.json (not just root tsconfig.json)
- Hono sub-router params from parent routes (e.g., `:libraryId` in parent `/:libraryId/sources`) type as `string | undefined` — use `as string` cast; mount sub-routers with `router.route("/:parentParam/sub", makeSubRouter(db))`
- Drizzle boolean `{ mode: "boolean" }` returns JS `true`/`false`, not `1`/`0` — use `toBe(false)` in tests, not `toBe(0)`
- `tsc --build --noEmit` fails in TypeScript 5.9 with chained composite project references — use root `yarn typecheck` (`tsc --build`) for type checking
- Workspace packages already built (dist/ exists) don't need tsconfig.json references; adding them breaks `--noEmit` mode
- Biome `noImplicitAnyLet`: uninitialized `let` variables need explicit type annotations
- `.txt` is in the Documents media category — never a safe "non-media" test file; use `.log` or `.xyz`
- `vi.mock(module)` is hoisted; use `await import(module)` after mock to get typed mocked reference; flush microtasks with 2x `await Promise.resolve()`
- `exactOptionalPropertyTypes: true`: can't assign `T | undefined` to optional `T` property — use `if (typeof x === "string") obj.prop = x` pattern
- Biome `useLiteralKeys`: use dot notation (`obj.snake_case`) not bracket notation (`obj["snake_case"]`) for `Record<string, unknown>` access
- Mocking `node:child_process` spawn: create EventEmitter with `.stdout` EventEmitter, cast to `ReturnType<typeof spawn>`
- `music-metadata` `parseFile()` returns `{ common, format }` — `common.track`/`common.disk` always present with `.no: number | null`; `common.genre` is `string[]`; mock with `vi.mock('music-metadata', () => ({ parseFile: vi.fn() }))`, cast return as `never`
- Mock `node:fs/promises` with `vi.mock("node:fs/promises", () => ({ readFile: vi.fn() }))` alongside `node:child_process` mocks — both work independently per test file
- ZIP binary parsing: search for EOCD sig `0x06054b50` from end of file; CD entries at cdOffset each start with `0x02014b50` with uncompressed size at offset+24, filename length at offset+28 (46-byte fixed header + variable name/extra/comment)
- TAR parsing: 512-byte blocks; filename at 0-99, size in octal at 124-135, typeflag at 156 (0x30='0' or 0 = regular file); USTAR prefix at 345 when "ustar" magic at 257; `gunzipSync` from `node:zlib` to decompress .tgz before parsing
- `sharp` ESM default import; mock as `vi.mock("sharp", () => ({ default: vi.fn() }))`; use `.clone()` per size for batch-safe thumbnail generation; types bundled with sharp (no @types/sharp needed)
- Drizzle `ALTER TABLE ADD COLUMN` migrations generated cleanly via `yarn workspace @xon/server-core db:generate` after schema update
- When mocking sequential spawns (ffprobe then ffmpeg), use `mockImplementation` with `Promise.resolve().then(...)` for event emission — `mockReturnValueOnce` creates the proc before spawn is called, causing setTimeout to fire before listeners attach
- `Promise.resolve().then(...)` fires as a microtask after synchronous listener registration — ideal for mock EventEmitter event timing in async tests
- Hono `c.body()` accepts `Uint8Array<ArrayBuffer>` not `Buffer<ArrayBufferLike>` — use `new Uint8Array(buffer)` to convert Node.js Buffer for binary responses
- Biome `noNonNullAssertion`: avoid `arr[0]!` — use `const x = arr[0]; if (!x) return;` pattern instead
- `@hono/node-server` v1.x has no WebSocket support — use `ws` npm package with `noServer: true` + `server.on("upgrade", ...)` to handle WebSocket upgrades alongside Hono HTTP routes
- Module-level EventEmitter pattern (`eventBus`) decouples event producers from consumers; use `emitEvent(event: XonEvent)` wrapper for type safety
- `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync()` for testing setInterval scheduling without real time; remember `vi.useRealTimers()` in afterEach
- Mock `node:fs` with `vi.mock("node:fs", () => ({ watch: vi.fn() }))` to capture fs.watch callbacks; use `mockImplementation` to capture callback for debounce tests
- Biome `noNonNullAssertion` in test files: use `?.()` instead of `!()` for optional function calls (after `expect().toBeDefined()`)
- `fs.watch` (from `node:fs`, not `node:fs/promises`) for filesystem watching; supports `{ recursive: boolean }` options
- Frontend (apps/web): Vite 6 + React 19 + React Router 7 + Zustand 5; tsconfig uses `moduleResolution: "bundler"`, `lib: ["ES2022","DOM","DOM.Iterable"]`, `jsx: "react-jsx"`, `noEmit: true`; excluded from root tsconfig to avoid NodeNext/JSX conflicts
- Root tsconfig.json exclude must include `apps/web` to prevent root `tsc --build` from applying NodeNext module resolution to browser/JSX files
- CSS Modules with `noUncheckedIndexedAccess: true`: `styles.foo` returns `string | undefined`; use `styles.foo ?? ""` for direct className props or use template literals (always return `string`)
- Add `src/vite-env.d.ts` with `/// <reference types="vite/client" />` to enable CSS Module imports in TypeScript
- React Router v7 layout routes: `<Route element={<Layout />}>` wrapping child routes; `<Outlet />` renders child content inside the layout

---

## 2026-03-27 - US-007
- Defined media_items table with Drizzle ORM in server-core
- Files changed: packages/server-core/src/schema.ts, src/schema.test.ts, src/index.ts, drizzle/0001_loving_excalibur.sql
- mediaItems: id (pk), libraryId (FK→libraries CASCADE), dataSourceId (FK→dataSources CASCADE), filePath, fileName, fileSize, mimeType, mediaCategory, title, description, metadata (text/JSON default "{}"), drmProtected (boolean default false), createdAt, updatedAt, scannedAt (nullable)
- Indexes on libraryId, mediaCategory, and filePath using `index()` from drizzle-orm/sqlite-core
- 6 new tests: insert/retrieve, defaults, JSON metadata, drmProtected flag, cascade delete via library, cascade delete via data source
- **Learnings for future iterations:**
  - Drizzle SQLite indexes: use `index("name").on(table.col)` in the third arg of `sqliteTable()` as an array
  - Biome trailing comma: in arrow-function table definitions, trailing comma after `]` inside `()` gets removed — run `biome check --write` after adding indexed tables
  - Cascade delete from parent (library) also cascades through data_source to media_items (multi-level FK cascade)
---

## 2026-03-27 - US-006
- Defined libraries and data_sources tables with Drizzle ORM in server-core
- Files changed: packages/server-core/src/schema.ts (new), src/migrate.ts (new), src/schema.test.ts (new), src/index.ts (exports), drizzle/0000_curious_unus.sql (generated migration)
- libraries: id (pk), name, description, allowedMediaTypes (text/JSON), createdAt, updatedAt with `(unixepoch())` defaults
- dataSources: id (pk), libraryId (FK→libraries, CASCADE DELETE), type (local|network enum), path, recursive, enabled, timestamps
- migrateDatabase(db) applies migrations via drizzle-orm/libsql/migrator; path resolved with import.meta.url so it works in both src/ and dist/
- 6 new tests: library CRUD, JSON default, timestamp defaults, data source CRUD, network type, cascade delete
- **Learnings for future iterations:**
  - `migrate()` from drizzle-orm/libsql/migrator reads migration SQL files from disk; use `join(dirname(fileURLToPath(import.meta.url)), "../drizzle")` for path resolution
  - SQLite cascade delete requires foreign key pragma to be enabled at runtime; libSQL enables it by default
  - Biome: type imports must be sorted before value imports within the same source group
---

## 2026-03-27 - US-004
- Created packages/server-core with Hono HTTP server and boot function
- Files changed: packages/server-core/package.json, tsconfig.json, src/app.ts, src/server.ts, src/index.ts, src/index.test.ts, root tsconfig.json (added reference), yarn.lock
- Hono app with /api/v1/ basePath; GET /api/v1/health returns `{status:"ok", timestamp:ISO}`
- boot() reads PORT from process.env (via Node --env-file), defaults to DEFAULT_PORT (32400)
- Graceful shutdown on SIGTERM/SIGINT via server.close()
- 4 tests passing with vitest using app.request() (no real server needed)
- **Learnings for future iterations:**
  - Biome requires `process.env.PORT` (dot notation), not `process.env["PORT"]` (computed key)
  - Hono's `app.request()` enables in-process HTTP testing without starting a real server
  - Package tsconfig.json needs its own `references` array when depending on other workspace packages
---

## 2026-03-27 - US-001
- Implemented monorepo scaffold with Yarn 1.22 workspaces
- Files changed: package.json, tsconfig.json, biome.json, yarn.lock, .nvmrc (pre-existing), .gitignore (pre-existing)
- Created workspace directories: apps/, packages/, plugins/
- **Learnings for future iterations:**
  - .nvmrc already existed with v24.14.0 (not Node 22.x as PRD stated)
  - tsconfig.json has no references[] yet — add entries as packages are created
  - Yarn 1.22.22 is installed and working
  - `tsc --build` works at root level for typecheck
  - Empty workspace directories are not committed to git (need .gitkeep or content)
---

## 2026-03-27 - US-003
- Created packages/media-types with all 20 category definitions, MIME mappings, and helper function
- Files changed: packages/media-types/package.json, packages/media-types/tsconfig.json, packages/media-types/src/categories.ts, packages/media-types/src/index.ts, packages/media-types/src/index.test.ts, tsconfig.json (added reference), yarn.lock
- CATEGORY_DEFINITIONS: Record<MediaCategory, {extensions, mimeTypes}> for all 20 categories
- EXTENSION_TO_MIME and EXTENSION_TO_CATEGORY derived maps (first-category-wins for overlapping extensions)
- getMediaCategory(filePath): uses node:path extname, case-insensitive, returns MediaCategory | undefined
- 29 tests passing with vitest
- **Learnings for future iterations:**
  - node:path requires @types/node devDependency (not present at root level)
  - vitest works out-of-the-box with TypeScript/ESM — no separate tsconfig needed for tests
  - biome check --write applies format fixes; long arrays get reformatted to multi-line (lineWidth: 100)
  - noUncheckedIndexedAccess affects Record<string, T> access — always use `?? fallback`
---

## 2026-03-27 - US-002
- Created packages/shared with core TypeScript types and constants
- Files changed: packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts, packages/shared/src/types.ts, packages/shared/src/constants.ts, tsconfig.json (added reference), yarn.lock
- Types defined: Library, DataSource, MediaItem, User (all with proper fields per PRD)
- Constants: DEFAULT_PORT=32400, MediaCategory enum (20 values), UserRole enum (admin/user/guest)
- **Learnings for future iterations:**
  - ESM packages require .js extensions in import paths (even for .ts source files)
  - Package exports must use ./dist/index.js paths (not ./src/index.ts)
  - `yarn check` validates workspace dep graph; use `yarn biome check <dir>` for linting
  - Adding a package: create files → update root tsconfig.json references → `yarn install`
---

## 2026-03-27 - US-005
- Configured SQLite database with libSQL driver and Drizzle ORM in server-core
- Files changed: packages/server-core/package.json, src/db.ts, src/db.test.ts, src/index.ts, drizzle.config.ts, yarn.lock
- `openDatabase(url?)` async factory: creates @libsql/client, enables WAL via PRAGMA (skipped for :memory:), returns { client, db }
- Default URL: `file:${DATA_DIR ?? "./data"}/xon.db` (reads DATA_DIR env var)
- drizzle.config.ts: dialect sqlite, schema ./src/schema.ts, out ./drizzle — ready for US-006 schema
- db:generate and db:migrate scripts added to package.json
- `openDatabase` and `LibSQLDatabase` type exported from package index
- 4 new tests (in-memory, drizzle methods, WAL mode with temp file, DATA_DIR env var)
- **Learnings for future iterations:**
  - drizzle-orm/libsql `drizzle(client)` accepts @libsql/client directly (no adapter wrapper needed)
  - WAL mode must skip `:memory:` URLs — pragma doesn't apply to in-memory databases
  - Biome import order: node: built-ins always sort before third-party packages
  - `delete process.env.X` triggers Biome lint/performance/noDelete — use `process.env.X = undefined` instead
---

## 2026-03-27 - US-024
- Implemented static file serving for the web client
- Files changed: packages/server-core/src/staticFiles.ts (new), src/server.ts (uses WEB_CLIENT_DIR env var)
- `makeStaticMiddleware(dir)` returns a Hono MiddlewareHandler that: reads files from `dir`, sets Content-Type from extension map, sets Cache-Control (no-cache for HTML, immutable for /assets/*, short cache for others), falls back to index.html for SPA routing
- `server.ts`: reads `WEB_CLIENT_DIR` from env; if set, wraps API app in root Hono instance and adds static middleware
- Hashing detection: files under `/assets/` (Vite convention) get `public, max-age=31536000, immutable`; HTML gets `no-cache`
- No new tests (acceptance criteria only required typecheck, not test pass)
- **Learnings for future iterations:**
  - Hono middleware must skip API paths (`/api/`) to avoid intercepting API requests; check `urlPath.startsWith("/api/")`
  - For composing a root Hono app with basePath sub-apps: create `new Hono()`, call `app.route("/", apiApp)`, then `app.use("/*", middleware)`
  - Biome import sorting: local imports (./scheduler.js before ./staticFiles.js) go alphabetically
---

## 2026-03-27 - US-029
- Added list/table view toggle to LibraryBrowser with sortable columns
- Files changed: apps/web/src/pages/LibraryBrowser.tsx (view toggle, sort state, list rendering), LibraryBrowser.module.css (table/toggle/skeleton row styles), apps/web/src/components/MediaCard.tsx (added listView prop + tr-based rendering, added fileSize/createdAt to MediaCardItem), MediaCard.module.css (list row styles)
- View mode persisted in Zustand `useAppStore` (already had `viewMode`/`setViewMode`)
- List view: table with small thumbnail, title (+ file type badge), category, size (formatted bytes), date added (locale date)
- Sortable columns: title, fileSize, createdAt (client-side toggle asc/desc + re-fetch with sortBy/order params); mediaCategory falls back to createdAt (API doesn't support it directly)
- Skeleton loading in list view: 10 skeleton rows with shimmer animation
- **Learnings for future iterations:**
  - Biome lint `noNoninteractiveTabindex` disallows tabIndex on `th` — use onClick + onKeyDown (Enter/Space) without tabIndex for accessible interactive table headers
  - Biome formatter is strict about trailing commas in template literals inside fetch() — let biome --write auto-fix formatting issues
  - `MediaCardItem` can render as either a grid card (`<Link>`) or list row (`<tr>`) via a `listView` prop — the component must return the right element type for table context
---
