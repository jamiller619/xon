## Codebase Patterns
- Monorepo with Yarn 1.22 workspaces: apps/*, packages/*, plugins/*
- TypeScript with strict mode, NodeNext module resolution (ESM)
- Biome for linting/formatting (not ESLint/Prettier)
- Node version: v24.14.0 (despite PRD saying Node 22.x LTS, .nvmrc had v24.14.0)
- tsconfig.json uses composite: true for project references support
- Each new package needs: package.json (type:"module", exports with .js extensions), tsconfig.json (extends root), src/index.ts
- When adding a package, add its path to root tsconfig.json "references" array and run `yarn install`
- Use `yarn biome check <dir>` (not `yarn check`) for linting; `yarn check` validates workspace deps
- Node built-ins (e.g., node:path) require `@types/node` as a devDependency in each package that uses them
- Test framework: vitest (^2.1.0) — handles TypeScript/ESM natively without separate tsconfig; add per-package
- noUncheckedIndexedAccess: Record<string, T> indexing returns T | undefined — use `?? fallback` pattern
- Biome line width is 100; arrays/params exceeding this are auto-reformatted to multi-line on `biome check --write`

---

## 2026-03-27 - US-001
- Implemented monorepo scaffold with Yarn 1.22 workspaces
- Files changed: package.json, tsconfig.json, biome.json, yarn.lock, .nvmrc (pre-existing), .gitignore (pre-existing)
- Created workspace directories: apps/, packages/, plugins/
- **Learnings for future iterations:**
  - .nvmrc already existed with v24.14.0 (not Node 22.x as PRD stated)
  - tsconfig.json has no references[] yet — add entries as packages are created
  - Yarn 1.22.22 is installed and working
  - `tsc --build` works at root level for typecheck
  - Empty workspace directories are not committed to git (need .gitkeep or content)
---

## 2026-03-27 - US-003
- Created packages/media-types with all 20 category definitions, MIME mappings, and helper function
- Files changed: packages/media-types/package.json, packages/media-types/tsconfig.json, packages/media-types/src/categories.ts, packages/media-types/src/index.ts, packages/media-types/src/index.test.ts, tsconfig.json (added reference), yarn.lock
- CATEGORY_DEFINITIONS: Record<MediaCategory, {extensions, mimeTypes}> for all 20 categories
- EXTENSION_TO_MIME and EXTENSION_TO_CATEGORY derived maps (first-category-wins for overlapping extensions)
- getMediaCategory(filePath): uses node:path extname, case-insensitive, returns MediaCategory | undefined
- 29 tests passing with vitest
- **Learnings for future iterations:**
  - node:path requires @types/node devDependency (not present at root level)
  - vitest works out-of-the-box with TypeScript/ESM — no separate tsconfig needed for tests
  - biome check --write applies format fixes; long arrays get reformatted to multi-line (lineWidth: 100)
  - noUncheckedIndexedAccess affects Record<string, T> access — always use `?? fallback`
---

## 2026-03-27 - US-002
- Created packages/shared with core TypeScript types and constants
- Files changed: packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts, packages/shared/src/types.ts, packages/shared/src/constants.ts, tsconfig.json (added reference), yarn.lock
- Types defined: Library, DataSource, MediaItem, User (all with proper fields per PRD)
- Constants: DEFAULT_PORT=32400, MediaCategory enum (20 values), UserRole enum (admin/user/guest)
- **Learnings for future iterations:**
  - ESM packages require .js extensions in import paths (even for .ts source files)
  - Package exports must use ./dist/index.js paths (not ./src/index.ts)
  - `yarn check` validates workspace dep graph; use `yarn biome check <dir>` for linting
  - Adding a package: create files → update root tsconfig.json references → `yarn install`
---
